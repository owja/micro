{
  "name": "demos",
  "version": "0.0.0",
  "dependencies": {
    "express": "^4.18.2"
  },
  "source": "src/demos.ts",
  "exports": "./dist/demos.mjs",
  "scripts": {
    "start": "npm run build && npm run serve",
    "build": "microbundle -f modern --sourcemap false --generateTypes false --external none",
    "serve": "node serve.js"
  },
  "devDependencies": {
    "@owja/micro": "^1.0.0-alpha.1",
    "@owja/typescript-config": "^1.0.2",
    "microbundle": "^0.15.1"
  }
}
